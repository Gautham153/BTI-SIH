// Bharat Tender Intelligence (BTI) — Auth Hook Export
// Phase 1B: Centralized Hook for Authentication & RBAC

export { useAuth } from '../context/AuthContext';
export type { AuthContextType } from '../context/AuthContext';
